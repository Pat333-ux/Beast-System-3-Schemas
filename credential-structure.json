{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nwo-dao.org/schemas/identity/credential-structure.json",
  "title": "CredentialStructure",
  "description": "SAIA-Class S credential structure defining the format for identity, role, authority, and program credentials within Beast System 3.0.",
  "type": "object",

  "properties": {
    "credentialId": {
      "type": "string",
      "description": "Unique identifier for the credential (UUID recommended)."
    },

    "type": {
      "type": "string",
      "enum": [
        "identity",
        "role",
        "authority",
        "training",
        "program",
        "verification",
        "membership"
      ],
      "description": "Type of credential being issued."
    },

    "subjectId": {
      "type": "string",
      "description": "Identity ID of the credential holder."
    },

    "issuerId": {
      "type": "string",
      "description": "Identity ID of the issuer (system, sovereign, or authorized
